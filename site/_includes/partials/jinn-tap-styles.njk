<link rel="stylesheet" href="{{ '/assets/jinn-tap.css' | prefixUrl }}">
<link rel="stylesheet" href="{{ '/css/embed.css' | prefixUrl }}">
{# Format document styles (tei-/jats-editor-styles.css) are injected by <jinn-tap>
   from the schema's `css` property — no need to link them here. #}
